const usuarios = ['alice', 'maria', 'joaquim', 'ester', 'joao', 'lia']
for (let nome of usuarios) {
    if (nome === 'joao') {
        console.log ('encontrado')
        break
    }
    console.log ('não encontrado')
    
    
}